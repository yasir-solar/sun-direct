import type{MetadataRoute}from"next";import{site}from"@/data/site";import{pages,blogPosts}from"@/data/content";export const dynamic="force-static";export default function sitemap():MetadataRoute.Sitemap{const paths=["",...pages.map(x=>x.slug),"our-process","projects","blog",...blogPosts.map(x=>`blog/${x.slug}`),"contact","free-solar-assessment"];return paths.map((x,i)=>({url:`${site.url}/${x}`,lastModified:new Date("2026-07-21"),changeFrequency:i===0?"weekly":"monthly",priority:i===0?1:(x.includes("privacy")||x.includes("terms"))?0.3:0.7}))}
